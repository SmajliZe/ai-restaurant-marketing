# Workspace packages must resolve to the local checkout, never to the registry.
link-workspace-packages=true
prefer-workspace-packages=true

# Peers of the framework packages (React, Next) are plentiful and mostly benign;
# failing installs on them costs more than it catches.
strict-peer-dependencies=false
auto-install-peers=true
